{
  "method": "crm.contact.list",
  "required": ["filter"],
  "properties": {
    "filter": {
      "type": "object",
      "description": "Фильтр по контактам, например {\"NAME\": \"Иван\"}."
    },
    "select": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Список полей для возврата."
    },
    "order": {
      "type": "object",
      "description": "Сортировка, например {\"ID\": \"DESC\"}."
    }
  },
  "description": "Возвращает список контактов."
}
