{
  "method": "batch",
  "required": ["cmd"],
  "properties": {
    "cmd": {
      "type": "object",
      "description": "Набор команд batch, ключ — алиас, значение — строка метода с параметрами."
    }
  },
  "description": "Выполняет пакетный запрос Bitrix24."
}
