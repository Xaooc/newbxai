# Отчёт по нагрузочному тестированию

- Скрипт: `python tools/load_test.py`
- Конфигурация: 12 параллельных пользователей, 60 итераций, адаптивный пул потоков.
- Результаты:
  - Среднее время ответа: 0.032 с
  - 95-й перцентиль: 0.053 с
  - 99-й перцентиль: 0.061 с

Запуск осуществлялся в тестовом окружении внутри контейнера, без реальных вызовов Bitrix24 (используется заглушка модели и состояния).
